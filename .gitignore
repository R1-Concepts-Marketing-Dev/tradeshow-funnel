node_modules/
.env
*.local
# Ingest working files may contain personal data — never commit them.
inbox/
out/
# Show reports carry spend figures and downloaded ad creative.
exports/
# The published page is a build artifact — it belongs wherever you host it,
# not in this repo's history.
published/
*.rejects.csv
